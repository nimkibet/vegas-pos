# POS System Configuration
# Terminal Mode: MASTER or CLIENT
terminal.mode=MASTER

# Master Terminal IP (only used when terminal.mode=CLIENT)
master.ip=192.168.1.100
master.port=3306

# Cloud API URL
cloud.api.url=https://api.example.com

# Database settings
database.name=pos_system.db

# Printer settings
printer.host=localhost
printer.port=9100
